package api

import (
	"testing"
)

func TestTable(t *testing.T) {
	// tb := new(Table)

	// dfTb := new(model.Table)
	// tb.setData(dfTb)
	// assert.Equal(t, tb.GetHandle(), "")

	// tb.setHandle("handler")
	// assert.Equal(t, tb.GetHandle(), "handler")

	// assert.Equal(t, tb.String(), "table[0r][0c]([\n\t])")
}
